import { TestBed, async } from '@angular/core/testing';
import { AppComponent } from './app.component';

describe('App: Enceladus', () => {
  it('should create the app', async(() => {
    let a;

    it('and so is a spec', function () {
      a = true;
      expect(a).toBe(true);
    });
  }));
});
